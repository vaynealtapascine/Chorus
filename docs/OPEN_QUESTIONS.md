# Open questions

Non-blocking. Each has a **default** that implementers use until the owner answers. When answered,
move the answer into DECISIONS.md and delete it here. (Q1-Q5 and Q7-Q12 were answered 2026-09-23
and became D-045 to D-053.)

| # | Question | Default until answered |
| --- | --- | --- |
| Q6 | Custom emoji = small uploaded images used like emoji (`:kai_wave:`), Discord-style, usable in messages, posts and reactions. Wanted? If yes: one server-wide set, per-space sets, per-account sets, or all three? Also stickers (bigger images sent on their own)? | Server-wide set (admin) + per-space sets, no stickers. The data model reserves `custom_emoji` entities either way. |
